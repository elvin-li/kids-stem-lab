.DS_Store
*.log
__pycache__/
*.py[cod]
# tools/_pixel-proof.mjs 存整页截图哈希的地方，是本地临时产物、不该提交：
# 基线只对「拍它时的那一份共享层」有效，assets/css/base.css 一改就全部失效。
tools/.pixel-proof/
# tools/_med-style-snapshot.mjs 的 before/after 计算样式快照，同为一次性本地产物：
# 只服务于「抽 med.css 时排版必须完全不变」那次比对，比对结束就没有用了。
tools/.med-style/
# 语义评审本地落盘，不是站点业务文件。
semantic-review/
